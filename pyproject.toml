[project]
name = "limem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dashscope>=1.25.11",
    "kuzu>=0.11.3",
    "pydantic>=2.7.0",
    "python-dotenv>=1.0.1",
]
