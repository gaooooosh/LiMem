[project]
name = "limem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dashscope>=1.25.11",
    "graphviz>=0.21",
    "kuzu>=0.11.3",
    "python-dotenv>=1.0.1",
]
