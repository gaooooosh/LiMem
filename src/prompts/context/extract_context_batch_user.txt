待处理 item 总数：{item_total}

以下是待识别 Context 的 item 列表（item_index 从 0 开始）：
{items_json}

请为每个 item 返回一条结果，严格 JSON：
{{
  "items": [
    {{
      "item_index": 0,
      "contexts": [
        {{
          "subtype": "constraint",
          "summary": "主体指向的可复用背景标签",
          "description": "Agent 从该 item 的感知流中观察到某主体处于某种可复用背景，并说明证据线索",
          "confidence": 0.88,
          "evidence_span": "必须来自该 item 原始感知流的原文片段"
        }}
      ],
      "rejected_candidates": [
        {{
          "text": "被拒绝的片段",
          "reason": "intent_or_event_not_context"
        }}
      ]
    }}
  ]
}}

规则：
1. 返回数量必须等于 {item_total}，每个 item 恰好一条结果。
2. 每条结果只能使用对应 item 的 record_text 和 event，不要跨 item 混用信息。
3. Context 是 Agent 对感知流观察后归纳出的背景框架；不是原始数据本身。
4. 每个 Context 必须有明确主体；不同主体的背景必须分开。
5. subtype 只能是 situation、constraint、environment。
6. 不抽取当前意图、请求、计划、目标、动作、结果或低复用碎片。
7. 如果某个 item 没有高质量 Context，也必须返回对应 item_index，且 contexts 置为空数组。
8. evidence_span 必须是对应 item 原始感知流中的原文片段，不要改写。
9. summary 要短、可复用、去事件化，并包含主体指向。
10. description 要说明主体、背景类型、可复用价值，以及 Agent 从哪些感知线索推断得到。
11. 只输出 JSON，不要任何解释文本。

{existing_contexts_note}
