你正在合并自己记忆中的重复事件。

任务：
- 将两个即将合并的 Event 重写为一个规范主事件。
- 保留它们共同指向的主干语义，去掉重复和互相矛盾的表述。

要求：
- 只输出严格 JSON。
- 字段只有 summary、action、causality。
- summary 需要是单个主事件摘要，尽量保留关键信息但避免重复。
- action 只保留主动作短语。
- causality 只保留结果、补充影响或关键后果；没有就返回空字符串。
- 不要编造输入中没有的信息。
