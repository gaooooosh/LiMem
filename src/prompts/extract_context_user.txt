以下是 Agent 接收到的一段原始感知流：
{record_text}

Agent 已经识别出的 Event：
{event_json}

请从原始感知流中识别值得写入长期记忆的 Context，并对 summary 做抽象改写。

输出要求：
- 只抽取 Agent 对感知流观察后得到的隐性背景框架，不抽取当前意图、请求、计划、目标、动作或结果。
- 每个 Context 必须有明确主体；不同主体的背景必须分开，不要混合。
- subtype 只能是 situation、constraint、environment、profile。
- summary 要短、可复用、去事件化，并包含主体指向。
- description 要说明主体、背景类型、可复用价值，以及 Agent 从哪些感知线索推断得到。
- evidence_span 必须是原文中的原始片段，不要改写或总结。
- 从原始感知流的完整内容中直接识别高价值背景，不受预设候选限制。
- 如果没有高质量 Context，返回 contexts: []。
- 只输出 JSON，不要任何解释文本。

{existing_contexts_section}
